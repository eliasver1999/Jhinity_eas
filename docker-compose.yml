services:
  postgres:
    image: postgres:16
    container_name: eas-dashboard-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: eas_dashboard
    ports:
      - "5432:5432"
    volumes:
      - eas-dashboard-pg:/var/lib/postgresql/data

volumes:
  eas-dashboard-pg:
